# Project Update — Full Quaternion-Based Attitude Control for a Quadrotor

Group 12 · Introduction to Data-Driven Control of Drones · Amrita Vishwa Vidyapeetham
Branch: `sainath/sim-deployment` · Live: **quadrotor-quaternion-sim.vercel.app**

---

## What was proposed (FRP.md)

Take the paper's quaternion attitude controller (Fresk & Nikolakopoulos, ECC
2013) — **gains frozen**: P&#8347;=20, P&#969;=4, ±4 N·m — and fly it on **four
simulation stacks**: gym-pybullet-drones, MuJoCo, Gazebo, ArduPilot SITL, with
one shared bridge, one telemetry schema, one cross-simulator benchmark.
Milestone M1 = bridge + the two native adapters; M2 = Gazebo + ArduPilot.

## Status — all four stacks delivered

| Stack | Status | Evidence |
|---|---|---|
| **MuJoCo** (20 kHz, contact physics) | ✅ **validated natively** | all 3 paper scenarios; flip completes fully airborne (3 seeds, φ settles 2.87–2.97 s); CSVs + figures in `results/sim_mujoco/` |
| **gym-pybullet-drones** (24 kHz, custom paper-vehicle URDF) | ✅ **validated natively** | step/sine within **0.2° RMS of MuJoCo**; flip = documented engine finding; `results/sim_gym_pybullet/` |
| **Gazebo** (gz-harmonic 8.15 + ardupilot_gazebo + real firmware) | ✅ **VALIDATED — flown under WSL2** | full chain run: `sim/wsl/run_gazebo_one.sh`; all 3 scenarios; sine = 17.28° RMS, within 0.4° of the native stacks |
| **ArduPilot SITL** (real firmware, MAVLink SET_ATTITUDE_TARGET @ 50 Hz) | ✅ **VALIDATED — flown under WSL2** | compiled ArduCopter from source; arm at 1 s, takeoff, 3 scenarios; position hold holds drift to 0.03 m |

## Headline results

1. **Cross-engine agreement**: identical gains/references/noise give step &
   sine RMS within 0.2° between MuJoCo and PyBullet (17.7°/17.7°; 17.6°/17.5°)
   — the closed loop belongs to the controller, not the engine.
2. **The flip, honestly**: completes deterministically on MuJoCo (airborne);
   engine-dependent limit cycle on PyBullet — *derived* as the P² pursuit
   equilibrium ω = 5·sin(e/2) (Eq. 33), not hand-waved.
3. **Three derived contributions** (Eqs. 31–33 in `file_structure.md`): the
   q_paper = conj(q_sim) simulator convention (test-proven), PX4-style
   priority mixer desaturation (measured necessity), and the flip
   rate-equilibrium analysis.

Full benchmark table + 8 figures: repo README (results section), or
**quadrotor-quaternion-sim.vercel.app** — machine-generated by
`python -m sim.compare` / `sim.site_gen`, nothing hand-edited.

## Tests

**41 tests** (27 original core + 14 new): the bridge's convention proof,
priority mixer, adapter smoke tests, PyBullet actuation calibration, Gazebo
SDF validation, and the MAVLink end-to-end bridge flight.

## Demo plan (2 minutes)

```bash
git checkout sainath/sim-deployment
python -m sim.mujoco.run --scenario flip --gui          # the airborne 360°
python -m sim.gym_pybullet.run --scenario step          # cross-engine check
pytest tests/ -q                                        # 41 pass/skip-clean
```
Plus the live site: four-stack status cards → benchmark table → figures →
"Fly the interactive simulator" button.

## Next (to final evaluation)

1. ~~Run Gazebo + ArduPilot firmware~~ ✅ **done** — both flown under WSL2
   (Ubuntu 24.04, gz-harmonic, ArduCopter built from source; see
   `sim/wsl/`).
2. Merge `sainath/sim-deployment` → `main` for the final evaluation, per the
   course's branch rules.
